# Define function to display a welcome message
def disp():
    name = "Imran"  # Using "Imran" instead of "GeekyShows"
    print("Welcome to", name)

# Call the display function multiple times
disp()
disp()
disp()

# Divide a large task into smaller tasks, helpful for debugging
# Separate function for addition
def add():
    x = 10
    y = 20
    c = x + y
    print(c)
    
# Separate function for subtraction
def sub():
    x = 10
    y = 20
    c = y - x
    print(c)

# Call the addition and subtraction functions
add()
sub()
