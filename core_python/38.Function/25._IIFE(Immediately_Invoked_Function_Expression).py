#Immediately Invoked Function Expression
# Using lambda functions to perform operations and print results

# Example 1: Lambda function adding 1 to the input value and printing the result
(lambda x: print(x + 1))(5)

# Example 2: Lambda function adding two input values and printing the result
(lambda x, y: print(x + y))(5, 2)
