# Printing the header for the isspace function
print("****** isspace Function ******")

# Assigning the value "  " to variable 'name'
name = "  "

# Applying the isspace() function to check if all characters in 'name' are whitespace
str1 = name.isspace()

# Printing the original 'name' and the result of the isspace() function
print(name)
print(str1)
